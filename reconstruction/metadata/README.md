# Worker metadata

Each worker may add one JSON handoff per reconstructed function or type under this directory. The integrator reconciles those records into the canonical manifest. Do not overwrite another worker's handoff and do not write shared manifest fields from a worker.
