# Integrated reconstruction

This directory is reserved for integrator-owned source snapshots and batch handoffs after semantic and ABI/type review. It is not a worker staging area and is not a substitute for the repository `src/` tree.
