# 019 — Reconstruction phase: community-Ghidra reuse + first real cell campaign

Date: 2026-09-23. Status: **Done (PASS — playable Cell Stage slice; 31/32 campaign targets).**
Predecessor: chapter [018](018-s5-cell-runtime-evidence.md) (S5, negative).

Strategic direction changed for this phase: optimize for **speed to a playable
Spore successor** by reusing existing community Ghidra/ModAPI work, the local
Ghidra project, the OpenSpore KG, dossiers, and the original runtime as oracle —
not for a pristine research framework. Pipeline: REUSE → UNDERSTAND →
REIMPLEMENT → TEST → PLAYABLE. No clean-room policing; provenance preserved
(`origin=community|ghidra|us|runtime`, `source`, `revision`) on every ingested fact.

## Phase 1–2 — community Ghidra made directly usable

- Inventory (subagent 1): richest asset = local Ghidra project
  `~/ghidra-spore-project/SporeProject` (SporeApp.exe 3.1.0.22 GOG,
  sha256 `25d42a7a…914e`): 58,757 functions, 11,808 datatypes, 1,670 SDK-named
  functions, 1,895 SDK structures. Its `.gpr` pointer file was 0 bytes, but the
  GhidraMCP headless server (127.0.0.1:8089, Ghidra 12.1.2) already holds the
  project open in memory — **no repair needed; reused as-is**.
- Exports (subagent 2) at git-ignored `.spore-analysis/ghidra-exports/`:
  `functions.tsv` (58,757), `structs.tsv` (2,035) + `structs_fields.tsv` (25,546),
  `sdk_functions.tsv` (1,666 community names, Spore-ModAPI rev `cbf9206`),
  `decompiled_sdk/` (478 pseudocode files), `vtables.json` (3,081 candidates /
  306 SDK-associated), `spore_sdk.xml` provenance, `manifest.json`.

## Phase 3 — KG reconciled (no clobbering)

`knowledgegraph/spore.db`: 2,074 → **65,461 nodes**, 136 → **20,397 edges**,
15 → **25,561 fields**; investigations/dossiers untouched (1,681).
`tools/kg_ingest.py` (idempotent, stdlib, merge-never-overwrite, provenance per
row). Reconciliation: 0 name conflicts; 477/1,663 SDK addresses are live function
entries (989 absorbed into larger current functions, 197 uncovered); 3 stale
named nodes flagged (`no-function-entry-in-current-program`); cell-movement
dossier size drift recorded (8 functions, +1 byte each). Trace-node `rva`
values are absolute addresses, not base-relative (11/14 resolve cleanly).

## Phase 4–5 — real Spore map + high-value targets

- `docs/SPORE-RECONSTRUCTION-ROADMAP.md`: 20+ subsystems with scale/status/
  OpenSpore-coverage; ranked order (Cell first: unique head start + highest
  knowledge completeness + decode-side, not runtime-gated).
- `docs/analysis/campaigns/cell-stage-campaign.md`: 32 targets in 5 waves, each
  with original address, decompilation evidence, semantics, in-tree mapping,
  test strategy.

## Phase 6–8 — campaign executed (the first real reconstruction campaign)

Wave 1 (static unblockers): CS-01 movement plane constants VERIFIED from the
binary (`{0,0,1}` normal at image `0x015a7c40`; point is BSS, runtime-written);
contract `cell-sim-contract/1` → `/2`. CS-02 worldobj `0x0f43029a` full format
(magic `0xABB455B7`, ver 9/10, 1,365 records, variable-length entries; 0
invariant violations) + C++ `src/assets/WorldObject.{hpp,cpp}`. CS-03
CellSerializer core + `cCellGlobalsResource` (276-B record, type `0x2A3CE5B7`,
69-field direct serialization) + C++ `CellResource.{hpp,cpp}`. CS-04 RW4
container ported to C++ (`src/assets/Rw4.{hpp,cpp}`) — **byte-identical to the
python oracle on all 1,131 real Spore_Content records**.

Waves 2–4 (the playable slice): 8 cell resource decoders (cell/world/populate/
structure/loot/look/random/effect+backgroundMap) + player-cell identity (CS-13,
manifest-driven) + canonical asset manifest v1; sim core (ray-plane steering with
pinned constants, `cCellGame` state, 4096-cell object pool, per-cell layout,
`CreateCellObject`, query pool, advect scale→bucket, damage/scale/attack rules);
scene + assets (record-driven declarative `scene.json`, real cell GMDL family —
88 records, backgroundMap 12-stop ramp → clear `(0,90,152)`, `cCellGFX`
8-world table + record-key preload, `cCellUI` health HUD).

Wave 5 (lifecycle + verification): CS-28 `IGameMode` + `CellModeStrategy`
lifecycle mapped 1:1 to the original `cCellModeStrategy` vtable slots (10
resolved `App::cCellModeStrategy::*` slots, 27-slot vtable) + `GameModeManager`,
RAII teardown in `cell_stage`. CS-29 table-driven `CellInput`
(+ `virtualKey==2` health toggle). CS-30 +4 deterministic contract scenarios
(plane-world switch, advect settle, multi-prey combat, zoom-scale); fixtures
regenerated byte-identical; contract stays `/2`. CS-31 `CellAnim`
`PlayAnimation` blend state machine + event→effect mapping. CS-32 PARTIAL —
static scaffolding confirmed; trace sub-part BLOCKED (S5 human-watched; see
chapter 018: it ran and was NEGATIVE — the game self-exits under Wine before
Cell Stage).

## The slice (what is now true)

`build/src/apps/cell_stage` runs the original Cell-Stage pipeline end-to-end on
real, read-only GOG assets: decode (real cell GMDLs + backgroundMap ramp) → sim
(4096-cell pool, advect, combat) → GFX (world table, record-key preload,
main-model world `0x1010020` binding) → scene (declarative 6-entity
`scene.json`, per-value provenance) → render (Vulkan, textured cells + backdrop,
clear compositing, HUD overlay). Deterministic output: manifest + raster stats
(`content=94211` in window 60000–115000, `clear=(0,90,152)`, `distinct=10860`,
`clearish=167933`); graceful no-display smoke. Key finding: original cell
placement is **procedural** — populate records carry `zOffset`/distribution
parameters; no record stores per-entity x/y.

## Verification (independent reviewer, fresh context)

8 PASS / 1 PARTIAL / 1 FAIL (docs, fixed by this chapter's STATE.md edits):
ctest 36/36 + python suite 408 OK; double-run byte-identical; exports + live MCP
confirmed; KG counts exact; replacement diffs are genuine differentials (4
verified / 1 approx / 3 stub — earlier "6/3" claim corrected); no runtime
executed this phase (static-first honored); worker context pack self-sufficient;
S1/S2/S2.1 infra intact; git clean, no large blobs.

## Process (repeatable for the next system)

Per target: `python3 tools/recon_worker/recon_worker.py context <ID>` (complete
static evidence pack: plan entry, decompilation text, structs+fields, vtable
slots, KG nodes, in-tree mapping, oracles, test conventions) → implement → test →
`record <ID> --status …` (updates campaign doc + STATE.md + KG test row).
Worker: `tools/recon_worker/` (404 + 56 LOC, stdlib-only).

## Commits (this phase)

`a96602b` (roadmap + KG index + wave 1 start) → `db153d7` (wave 1 complete) →
`0618f79` (worker) → `91e43f5` (waves 2–4 slice) → `b458104` (wave 5) → this
chapter's docs commit.

## Next

1. **Wine boot-stability diagnostic (read-only)** — why does the SporeApp.exe
   process tree self-exit under Wine (run #1: ~12.7 s after menu; run #2: never
   boots)? This is the only remaining gate to the cell-mode trace and
   `replaced-verified` promotion.
2. **Asset pipeline completion** (roadmap rank #2): `.prop` property
   serialization round-trip + full `cResourceManager` (decompilations exist).
3. Creature stage prep: Anim (RW4 keyframe + TLSA) + Terrain — the IGameMode /
   mode-strategy / input / scene patterns from this campaign are designed to be
   reused verbatim.
